# Phase 7 - Snowflake port
**Goal:** spin up Snowflake, point the same dbt project at it, and exercise the cloud-DW-specific levers (clustering keys, search optimization, query profile inspection). The dispatch macros from Phases 5-6 mean the models don't change — only the target switches.

## Commands (step-by-step)
```bash

```